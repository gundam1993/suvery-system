export const getActiveSuvery = (state) => {
  return state.activeSuvery;
};

export const getActiveSuveryTitle = (state) => {
  return state.activeSuveryTitle;
};

export const getActiveSuveryQuestions = (state) => {
  return state.activeSuveryQuestions;
};

export const getSuverys = (state) => {
  return state.suverys;
};